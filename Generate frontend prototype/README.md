
  # Generate frontend prototype

  This is a code bundle for Generate frontend prototype. The original project is available at https://www.figma.com/design/zrU2KWom2ovaGqX1bmi1fZ/Generate-frontend-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  